// import Header from "../components/shared/Header";
import { GenProps } from "../utilities/GenProps";

const UnAuthTheme = ({ children }: GenProps) => {
  return (
    <>
      {/* <Header></Header> */}
      {children}
    </>
  );
};

export default UnAuthTheme;
